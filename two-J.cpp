#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
    int n;
    cin >> n;
    vector <vector <long double>> dp(n, vector <long double>(n));
    vector <int> a(n);
    for (int i = 0; i < n; ++i)
        cin >> a[i];
    for ()
}